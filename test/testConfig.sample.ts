import { ConnectionOptions } from '../src/interfaces/Options'

export default {
    host: 'localhost',
    port: 3306,
    user: 'root',
    password: 'password',
    database: 'mysqldump_test',
} as ConnectionOptions
